
    Function kip(num1 , num2){
        if (num1 % num2 == 0) {
            console.log(true)}
            else{
                console.log("num1 is not a multiple of num2)
            }

    }
    console.log(kip(5,2));//function , function_name , (parameter1 , parameter2 , parameter_n) {
        //function body.
        //console.log(2);
        return parameter1 + parameter2;
    }
        //This statement may not be executed (parse)

        function devisor (a,b) {
            if (a%b ===0) {
                return true;
                {else}
                console.log(`$(a) is not a multiple of $(b)`);


                let our_person = function() {
                    consol.log("our friend`s name is " + person_obj.name... + "and he is" + person_obj.age);
                    }
                }
let person_obj = {
    name: 'ethiopia';
    age: 56;
    {

